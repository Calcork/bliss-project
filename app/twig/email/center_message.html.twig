<!DOCTYPE html>
<html dir="{{ ltr ? 'ltr' : 'rtl' }}">
<head>

    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width">
    <title>{{ site_name }}</title>

    <body>
        <div>{% block header_inner %} {% endblock %}</div>
        {% block center_inner %}
            <div>{{ center_message|raw }}</div>
        {% endblock %}
        <div>{% block footer_inner %}{% endblock %}</div>
    </body>

</head>
</html>